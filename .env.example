# === Настройки Telegram-бота ===
# Токен, полученный от @BotFather
BOT_TOKEN=123456:ABC-DEF1234567890

# === Настройки базы данных (для docker-compose) ===
# Строка подключения к PostgreSQL.
# Формат: postgresql+asyncpg://<USER>:<PASSWORD>@<HOST>:<PORT>/<DB_NAME>
DATABASE_URL=postgresql+asyncpg://user:password@db:5432/dbname

# === Настройки взаимодействия сервисов ===
# Внутренний базовый URL, по которому бот обращается к FastAPI-бэкенду внутри сети Docker.
API_BASE_URL=http://web:8000

# === Системные флаги ===
# Флаг, указывающий, что приложение запущено внутри Docker-контейнера.
# Оставьте 1 для запуска через docker-compose.
IS_IN_DOCKER=1